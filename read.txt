postgresql://custom_user:custom_user!@#$%^&*()@ep-royal-dawn-a4sqbgod-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require



/*
In Neon, databases are stored on branches. By default, a project has one branch and one database.
You can select the branch and database to use from the drop-down menus above.

Try generating sample data and querying it by running the example statements below, or click
New Query to clear the editor.
*/
-- Step 1: Create the user
CREATE USER custom_user WITH PASSWORD 'custom_user!@#$%^&*()';

-- Step 2: Grant access to the database and schema
GRANT CONNECT ON DATABASE sales_report TO custom_user;
GRANT USAGE ON SCHEMA public TO custom_user;

-- Step 3: Grant SELECT on all current tables
GRANT SELECT ON ALL TABLES IN SCHEMA public TO custom_user;

-- Step 4: Ensure future tables also grant SELECT to this user
ALTER DEFAULT PRIVILEGES IN SCHEMA public
GRANT SELECT ON TABLES TO custom_user;
